<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base theme for the application without ActionBar (used for LoginActivity) -->
    <style name="Base.Theme.VIAMM" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <!-- Theme with ActionBar (used for MainActivity and other activities) -->
    <style name="Base.Theme.VIAMM.NoActionBar" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your theme here -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->

        <!-- Custom theme for status bar -->
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:statusBarColor">@color/transparent</item>
    </style>

    <style name="Theme.VIAMM" parent="Base.Theme.VIAMM" />
    <style name="Theme.VIAMM.NoAB" parent="Base.Theme.VIAMM.NoActionBar" />

    <style name="CustomAlertDialog" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:textColorPrimary">@color/white</item>
        <item name="android:textColor">@color/AlertDialogText</item>
        <item name="android:background">@color/AlertDialogBG</item>
    </style>

</resources>
